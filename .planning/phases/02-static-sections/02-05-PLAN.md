---
phase: 02-static-sections
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/components/FAQ.astro
  - src/components/Footer.astro
  - src/pages/index.astro
autonomous: false

must_haves:
  truths:
    - "User sees an FAQ section with 5 expandable items using details/summary (CSS-only accordion)"
    - "User sees a footer with brand column, 3 nav columns, divider, and copyright row"
    - "User sees the complete landing page with all 9 sections in order: Header, Hero, Features, SoundLibrary, SocialProof, Pricing, FAQ, Footer — separated by dividers"
    - "Page is fully responsive across desktop, tablet, and mobile"
  artifacts:
    - path: "src/components/FAQ.astro"
      provides: "5 expandable FAQ items using details/summary"
      min_lines: 30
    - path: "src/components/Footer.astro"
      provides: "Footer with brand, nav columns, copyright"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "Full page composition of all section components"
      min_lines: 20
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/*.astro"
      via: "Import and render all section components"
      pattern: "import.*from.*components"
    - from: "src/components/FAQ.astro"
      to: "src/styles/global.css"
      via: "Global summary marker removal styles"
      pattern: "details"
---

<objective>
Build the FAQ and Footer sections, then compose all sections into the final index.astro page. Verify the complete landing page visually.

Purpose: This completes Phase 2 by assembling all sections into the full landing page and verifying the result.
Output: FAQ.astro, Footer.astro, complete index.astro with all sections, visual verification.
</objective>

<execution_context>
@C:/Users/zepid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zepid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-sections/DESIGN-SPEC.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-01-SUMMARY.md
@.planning/phases/02-static-sections/02-02-SUMMARY.md
@.planning/phases/02-static-sections/02-03-SUMMARY.md
@.planning/phases/02-static-sections/02-04-SUMMARY.md
@src/styles/global.css
@src/layouts/Layout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ and Footer components</name>
  <files>src/components/FAQ.astro, src/components/Footer.astro</files>
  <action>
**FAQ.astro** — matching DESIGN-SPEC.md section 7:

Section container:
- `<section>` — vertical flex, items-center, gap-12, bg-surface (#FAFAFA)
- Padding: py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]

Section header:
- Label: "FAQ"
- Title: "Frequently Asked Questions" — Orbitron 48px
- Description: "Everything you need to know about Orbital." — Inter 18px

FAQ data array:
```typescript
import { ChevronDown } from "@lucide/astro";

const faqs = [
  { question: "What DAWs does Orbital support?", answer: "Orbital supports all major DAWs including Ableton Live, FL Studio, Logic Pro, Pro Tools, Cubase, Studio One, Reaper, and Bitwig. Available as VST3, AU, and AAX plugin formats for both Windows and macOS." },
  { question: "Do I get free updates?", answer: "Yes! Your purchase includes free lifetime updates. We regularly release new presets, features, and performance improvements at no additional cost." },
  { question: "Can I use Orbital commercially?", answer: "Absolutely. Your Orbital license covers both personal and commercial use. Any music you create with Orbital is 100% yours to release, sell, or license." },
  { question: "What are the system requirements?", answer: "Orbital requires Windows 10+ or macOS 11+, a 64-bit DAW, at least 4GB RAM (8GB recommended), and 500MB of free disk space for the full preset library." },
  { question: "How does the preset library work?", answer: "Orbital comes with 500+ professionally designed presets organized by category and genre. You can browse, preview, and morph between presets in real-time. Custom presets can be saved and shared." },
];
```

FAQ list container:
- `<div class="w-full max-w-[800px]">`

Each FAQ item using `<details>/<summary>`:
```html
<details class="border-b border-border group">
  <summary class="flex justify-between items-center py-6 cursor-pointer text-text-primary font-medium text-base">
    {question}
    <ChevronDown size={20} class="text-text-light transition-transform duration-200 group-open:rotate-180 shrink-0 ml-4" />
  </summary>
  <p class="pb-6 text-[15px] text-text-body leading-relaxed">{answer}</p>
</details>
```

Note: The global CSS already has `summary { list-style: none; }` from Plan 01, removing the default disclosure triangle. The last FAQ item should still have border-b for consistency (design spec says last has no bottom stroke — use a conditional: last item gets no border-b). Apply via index check in map.

---

**Footer.astro** — matching DESIGN-SPEC.md section 8:

Section container:
- `<footer>` — vertical flex, gap-12, bg-surface-footer (#F5F5F7)
- Padding: py-12 md:py-16 px-6 md:px-16 lg:px-[120px]

Footer main (horizontal on desktop, vertical on mobile):
- `<div class="flex flex-col lg:flex-row lg:justify-between gap-12">`

Brand column (lg:max-w-[300px]):
- Logo: "CreativAudio" — font-mono, text-[20px], font-semibold, text-text-primary, tracking-[2px]
- Description: "Next-generation audio synthesis tools for modern producers." — text-sm, text-text-body, mt-4, leading-relaxed

Three nav columns:
```typescript
const columns = [
  { title: "Product", links: ["Features", "Presets", "Pricing", "Changelog"] },
  { title: "Support", links: ["Documentation", "Tutorials", "Contact", "FAQ"] },
  { title: "Company", links: ["About", "Blog", "Careers", "Press Kit"] },
];
```

Each column:
- Title: `<h4 class="text-sm font-semibold text-text-primary mb-4">{title}</h4>`
- Links: `<ul class="flex flex-col gap-2.5">` with each `<li><a href="#" class="text-sm text-text-body hover:text-text-primary transition-colors">{link}</a></li>`

Footer divider: `<div class="w-full h-px bg-border"></div>`

Footer bottom:
- `<div class="flex flex-col sm:flex-row justify-between items-center gap-4">`
- Copyright: "© 2026 CreativAudio. All rights reserved." — text-xs, text-text-lighter (#999999)
- Legal links: "Privacy Policy", "Terms of Service", "Refund Policy" — text-xs, text-text-lighter, flex gap-6
  </action>
  <verify>Run `npm run build` — no errors. FAQ.astro has 5 questions with details/summary. Footer.astro has 3 nav columns and copyright. Grep for "What DAWs" and "Privacy Policy" confirms content.</verify>
  <done>FAQ.astro renders 5 expandable items using native details/summary with chevron rotation. Footer.astro renders brand column, 3 nav columns, divider, and copyright row. Both responsive.</done>
</task>

<task type="auto">
  <name>Task 2: Compose all sections into index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the current placeholder index.astro with the full page composition. Import all section components and render in order with SectionDividers between major sections.

```astro
---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import SectionDivider from "../components/SectionDivider.astro";
import Features from "../components/Features.astro";
import SoundLibrary from "../components/SoundLibrary.astro";
import SocialProof from "../components/SocialProof.astro";
import Pricing from "../components/Pricing.astro";
import FAQ from "../components/FAQ.astro";
import Footer from "../components/Footer.astro";
---
<Layout title="Orbital - Next-Generation Audio Synthesizer | CreativAudio">
  <Header />
  <main>
    <Hero />
    <SectionDivider />
    <Features />
    <SectionDivider />
    <SoundLibrary />
    <SectionDivider />
    <SocialProof />
    <SectionDivider />
    <Pricing />
    <SectionDivider />
    <FAQ />
  </main>
  <Footer />
</Layout>
```

Wrap content sections in `<main>` for semantic HTML. Header is outside main (it's a landmark). Footer is outside main (it's a landmark). Add `max-w-[1440px] mx-auto` to `<main>` to cap width on ultra-wide screens per design spec (1440px frame width).

Run `npm run build` after to verify the full page builds without errors.
  </action>
  <verify>Run `npm run build` — succeeds. Check dist/index.html exists and contains all section content. Verify file has all 8 component imports.</verify>
  <done>index.astro composes all 9 sections (Header, Hero, Features, SoundLibrary, SocialProof, Pricing, FAQ, Footer) with SectionDividers. Page builds successfully as complete landing page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete landing page</name>
  <files>src/pages/index.astro</files>
  <action>
Start the dev server with `npm run dev`. Present the user with a verification checklist for the complete landing page. This is a human-verify checkpoint — all implementation is complete from prior tasks.

What was built: The complete Orbital landing page with all 9 sections: Header (logo, nav, CTA), Hero (wordmark, tagline, product placeholder, DAW compatibility), Features (6 cards), Sound Library (5 preset rows), Social Proof (4 stats + 3 testimonials), Pricing (countdown placeholder + pricing card), FAQ (5 expandable items), and Footer (brand + nav columns + copyright).

How to verify:
1. Open http://localhost:4321
2. Scroll through the full page — verify all 9 sections render in order
3. Check Header: logo left, nav center (desktop), cart + BUY NOW right
4. Check Hero: stars, ORBITAL title, tagline, CTA button (orange gradient), product placeholder, DAW row
5. Check Features: 6 cards in 3-column grid with orange icons
6. Check Sound Library: 5 preset rows on #FAFAFA background
7. Check Social Proof: 4 stats row, 3 testimonial cards with star ratings
8. Check Pricing: countdown timer boxes (dark), pricing card ($149), feature checks, CTA
9. Check FAQ: click each question — it should expand/collapse with chevron rotation
10. Check Footer: brand column, 3 link columns, copyright row on #F5F5F7
11. Resize browser to mobile width (~375px) — verify everything stacks cleanly, no horizontal overflow
12. Check overall: colors match design spec (#FF5C00 accent, #111111 headings, #F0F0F2 cards)
  </action>
  <verify>User confirms visual appearance matches design spec or provides feedback for fixes.</verify>
  <done>User has approved the complete landing page visual appearance, responsiveness, and design fidelity.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `dist/index.html` contains content from all 9 sections
- FAQ items expand/collapse via native details/summary
- Page renders at 1440px width matching design spec proportions
- Page is responsive at 375px mobile width with no horizontal scroll
- All section backgrounds correct: white, #FAFAFA (library, FAQ), radial gradient (pricing), #F5F5F7 (footer)
</verification>

<success_criteria>
The complete landing page is assembled and visually verified. All 9 sections render in order, responsive, with design-spec-accurate colors, typography, and spacing. FAQ accordion works with CSS only. Page builds and deploys successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-05-SUMMARY.md`
</output>
