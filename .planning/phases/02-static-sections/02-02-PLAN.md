---
phase: 02-static-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Header.astro
  - src/components/Hero.astro
autonomous: true

must_haves:
  truths:
    - "User sees a header with CreativAudio logo, four nav links, cart icon, and BUY NOW button"
    - "User sees a hero section with social proof row, ORBITAL wordmark, tagline, subtitle, gradient CTA button, product visual placeholder, and DAW compatibility row"
    - "Header and hero are fully responsive (stacked on mobile, horizontal on desktop)"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Sticky header with logo, nav, cart icon, CTA"
      min_lines: 20
    - path: "src/components/Hero.astro"
      provides: "Hero with wordmark, tagline, CTA, product visual, compatibility row"
      min_lines: 50
  key_links:
    - from: "src/components/Header.astro"
      to: "@lucide/astro"
      via: "ShoppingCart icon import"
      pattern: "ShoppingCart"
    - from: "src/components/Hero.astro"
      to: "@lucide/astro"
      via: "ArrowRight, Star icon imports"
      pattern: "ArrowRight"
---

<objective>
Build the Header and Hero sections matching the Pencil design spec, with full responsiveness.

Purpose: These are the first things users see — the header provides navigation and brand identity, the hero establishes the product's visual identity and primary CTA.
Output: Header.astro, Hero.astro components ready for composition.
</objective>

<execution_context>
@C:/Users/zepid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zepid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-sections/DESIGN-SPEC.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-01-SUMMARY.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component</name>
  <files>src/components/Header.astro</files>
  <action>
Create `src/components/Header.astro` matching DESIGN-SPEC.md section 1 exactly.

**Structure:** `<header>` with three groups: logo (left), nav (center), actions (right).

**Desktop layout (lg+):**
- Horizontal flex, justify-between, items-center
- Padding: py-5 px-6 md:px-16 lg:px-20 (design spec: padding [20, 80])
- Background: white (#FFFFFF)

**Logo group:**
- "CreativAudio" text — font-mono (DM Mono), text-[20px], font-semibold (600), text-text-primary (#111111), tracking-[2px]

**Nav group (hidden on mobile, flex on lg):**
- Items: "PRODUCTS", "PRESETS", "SUPPORT", "ABOUT"
- Each: font-body (Inter), text-[13px], font-medium (500), text-text-secondary (#555555), tracking-[0.8px]
- Gap: gap-8 (32px)
- Each link is an `<a href="#section-id">` for future smooth scroll (use ids: #features, #presets, #support, #about)

**Right group:**
- Cart icon: `<ShoppingCart size={20} class="text-text-secondary" />` from @lucide/astro
- BUY NOW button: rounded-md (6px), bg-accent (#FF5C00), px-6 py-2.5 (padding [10, 24]), text-[13px] font-semibold text-white tracking-[0.5px]
- Gap between cart and button: gap-4 (16px)

**Mobile (below lg):**
- Show logo and right group only
- Hide nav links (no hamburger toggle in this phase — just hidden; toggle is Phase 3 interactivity)
- Optionally show a `Menu` icon from lucide on mobile (visible below lg, hidden on lg+) as a non-functional placeholder

**Sticky behavior:** Add `sticky top-0 z-50` and `border-b border-border` so header stays visible on scroll.
  </action>
  <verify>Run `npm run build` — no errors. File exists with ShoppingCart import, four nav items, BUY NOW button.</verify>
  <done>Header.astro renders the full header matching design spec: logo, nav (desktop), cart icon, BUY NOW CTA. Responsive: nav hidden on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero component</name>
  <files>src/components/Hero.astro</files>
  <action>
Create `src/components/Hero.astro` matching DESIGN-SPEC.md section 2. This is the most complex section.

**Section container:**
- `<section>` with vertical flex, items-center, gap-12 (48px), padding py-16 md:py-20 px-6 md:px-16 lg:px-20
- Background: Use a CSS gradient to simulate the hero background (no actual image yet — per research open question, use gradient placeholder). Apply: `bg-gradient-to-b from-[#FFF0E6] via-[#FFF0E6] to-white` with opacity styling.

**Social proof row (id: SX4vJ):**
- Horizontal flex, items-center, gap-2
- 5 `<Star size={14} class="text-accent fill-accent" />` icons (filled stars)
- "Trusted by 10,000+ producers" — Inter, text-sm, text-text-body (#666666)

**ORBITAL wordmark (id: NAFAZ):**
- Use Orbitron font as large text: `<h1 class="font-heading text-6xl md:text-7xl lg:text-8xl font-bold tracking-wider text-text-primary">ORBITAL</h1>`
- This substitutes for the SVG wordmark until/unless the actual SVG is extracted from the Pencil file. Orbitron gives a very similar look.

**Hero text group:**
- Tagline: "Next-Generation Audio Synthesis" — font-heading (Orbitron), text-base md:text-[20px], font-normal (400), text-text-body (#666666), tracking-[4px], uppercase
- Subtitle: "Orbital delivers three independent oscillator engines, a spectral effects suite, and intelligent modulation routing — all in a single, performance-optimized plugin." — Inter, text-base md:text-lg, text-text-body, leading-relaxed (1.6), text-center, max-w-[560px]

**CTA button:**
- `<a>` with: rounded-md (6px), bg-gradient-to-r from-accent to-accent-end, px-10 py-4, text-[15px] font-semibold text-white tracking-[1px], inline-flex items-center gap-2
- Text: "GET ORBITAL"
- `<ArrowRight size={18} />` icon after text

**Product visual placeholder:**
- Since no product image exists yet, create a placeholder: `<div class="w-full max-w-[900px] aspect-[9/5] rounded-xl border border-border bg-card relative overflow-hidden">`
- Inside, add a subtle gradient overlay div: `<div class="absolute inset-0 bg-radial-[circle_at_center] from-[#FF5C0010] to-transparent opacity-40"></div>`
- Add centered placeholder text: "Product Visual" in text-text-light

**DAW Compatibility row (id: zUJwo):**
- Horizontal flex, items-center, gap-4 md:gap-8, py-5, flex-wrap justify-center
- "Compatible with" — Inter, text-xs, text-text-light (#888888)
- Separator: `<span class="w-px h-4 bg-border-medium"></span>` (1x16 rect, #D4D4D8)
- Items: "VST3", "AU", "AAX", "Windows & macOS" — font-mono (DM Mono), text-[13px], font-medium (500), text-text-body (#666666)
- Between each item, add a separator span
  </action>
  <verify>Run `npm run build` — no errors. File exists with Star, ArrowRight imports. Contains "ORBITAL", "GET ORBITAL", "Compatible with", all 4 format tags.</verify>
  <done>Hero.astro renders: social proof stars, ORBITAL wordmark, tagline, subtitle, gradient CTA, product visual placeholder with glow overlay, and DAW compatibility row. Responsive layout stacks vertically on mobile.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Both component files exist in src/components/
- Header has ShoppingCart icon, 4 nav links, BUY NOW button
- Hero has Star icons, ORBITAL heading, GET ORBITAL CTA, compatibility row with VST3/AU/AAX
</verification>

<success_criteria>
Header and Hero sections are complete, responsive, and match the design spec. Components are ready to be imported into index.astro.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-02-SUMMARY.md`
</output>
