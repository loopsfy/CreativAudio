---
phase: 02-static-sections
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Features.astro
  - src/components/SoundLibrary.astro
autonomous: true

must_haves:
  truths:
    - "User sees a Features section with 6 cards in a 3-column grid (desktop) showing icon, title, and description"
    - "User sees a Sound Library section with 5 preset rows showing name, category, genre tag, and play icon"
    - "Both sections have the standard section header pattern (label, title, description)"
    - "Feature cards and preset rows are responsive (stack on mobile)"
  artifacts:
    - path: "src/components/Features.astro"
      provides: "6 feature cards in 2x3 grid with lucide icons"
      min_lines: 40
    - path: "src/components/SoundLibrary.astro"
      provides: "5 preset rows with play icons and genre tags"
      min_lines: 40
  key_links:
    - from: "src/components/Features.astro"
      to: "@lucide/astro"
      via: "Icon component imports for 6 feature icons"
      pattern: "Waves|Sparkles|Sliders"
    - from: "src/components/SoundLibrary.astro"
      to: "@lucide/astro"
      via: "Play and ArrowRight icon imports"
      pattern: "Play"
---

<objective>
Build the Features and Sound Library sections matching the Pencil design spec, with data-driven card/row rendering.

Purpose: These sections showcase Orbital's capabilities and preset library — core product marketing content.
Output: Features.astro, SoundLibrary.astro components ready for composition.
</objective>

<execution_context>
@C:/Users/zepid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zepid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-sections/DESIGN-SPEC.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-01-SUMMARY.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Features section component</name>
  <files>src/components/Features.astro</files>
  <action>
Create `src/components/Features.astro` matching DESIGN-SPEC.md section 3.

**Section container:**
- `<section id="features">` — vertical flex, items-center, gap-12 md:gap-16 (64px)
- Padding: py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]
- Background: white (default)

**Section header** (reusable pattern):
- Vertical flex, items-center, gap-4, text-center
- Label: "FEATURES" — text-xs (12px), font-semibold (600), text-accent (#FF5C00), tracking-[2px], uppercase
- Title: "Designed for the Future of Sound" — font-heading (Orbitron), text-3xl md:text-[48px] (48px), font-normal (400), text-text-primary (#111111), tracking-tight (-1px)
- Description: "Orbital combines cutting-edge synthesis technology with an intuitive interface, giving you unprecedented control over your sound." — text-base md:text-lg (18px), text-text-body (#666666), leading-relaxed (1.6), text-center, max-w-[600px]

**Feature data array** (in frontmatter):
```typescript
import { Waves, Sparkles, Sliders, Shuffle, Monitor, Zap } from "@lucide/astro";

const features = [
  { icon: Waves, title: "3 Oscillator Engines", description: "Three independent oscillator engines with wavetable, subtractive, and FM synthesis modes for limitless sound design." },
  { icon: Sparkles, title: "Spectral Effects Suite", description: "Built-in spectral processing with reverb, delay, chorus, and our signature Orbital shimmer effect." },
  { icon: Sliders, title: "Intelligent Modulation", description: "Smart modulation routing that learns your style and suggests creative parameter connections." },
  { icon: Shuffle, title: "Preset Morphing", description: "Seamlessly blend between any two presets in real-time for evolving, dynamic soundscapes." },
  { icon: Monitor, title: "Visual Feedback Engine", description: "Real-time waveform visualization and spectrum analysis for precise sound design feedback." },
  { icon: Zap, title: "Built for Performance", description: "Optimized DSP engine with near-zero latency, designed to handle complex patches without dropouts." },
];
```

**Grid layout:**
- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full max-w-[1200px]`
- Map over features array

**Each card** (design spec: cornerRadius 12, fill #F0F0F2, stroke #E5E5E7 1px, padding 32, gap 20):
- `<div class="rounded-xl bg-card border border-border p-8 flex flex-col gap-5">`
- Icon: `<Icon size={24} class="text-accent" />`
- Title: `<h3 class="text-lg font-semibold text-text-primary">{title}</h3>`
- Description: `<p class="text-[15px] text-text-body leading-relaxed">{description}</p>`
  </action>
  <verify>Run `npm run build` — no errors. File contains 6 feature items with correct icon imports. Grep for "3 Oscillator Engines" and "Built for Performance" confirms all 6 cards.</verify>
  <done>Features.astro renders 6 cards in a responsive 3-column grid with lucide icons, titles, and descriptions matching the design spec.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sound Library section component</name>
  <files>src/components/SoundLibrary.astro</files>
  <action>
Create `src/components/SoundLibrary.astro` matching DESIGN-SPEC.md section 4.

**Section container:**
- `<section id="presets">` — vertical flex, items-center, gap-12 (48px)
- Padding: py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]
- Background: bg-surface (#FAFAFA)

**Section header:**
- Label: "SOUND LIBRARY" — same pattern as Features
- Title: "Explore the Sound" — Orbitron 48px
- Description: "Browse through 500+ professionally designed presets spanning every genre and style." — Inter 18px

**Preset data array:**
```typescript
import { Play, ArrowRight } from "@lucide/astro";

const presets = [
  { name: "Cosmic Drift", category: "Ambient Pad", genre: "Ambient" },
  { name: "Neural Bass", category: "Bass", genre: "Dubstep" },
  { name: "Quantum Pad", category: "Pad", genre: "Cinematic" },
  { name: "Digital Rain", category: "Arpeggio", genre: "Electronica" },
  { name: "Void Lead", category: "Lead", genre: "Synthwave" },
];
```

**Preset list container:**
- `<div class="flex flex-col gap-0.5 w-full max-w-[1200px]">` (gap: 2px from spec)

**Each preset row** (design spec: horizontal, cornerRadius 8, fill #F0F0F2 alternating with #F5F5F7, space-between, padding [20, 24]):
- `<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between rounded-lg bg-card p-5 sm:px-6 gap-3 sm:gap-0">`
- Use alternating backgrounds: even items get `bg-[#F5F5F7]`, odd items get `bg-card` (#F0F0F2). Use index in map.
- **Left side:** flex, items-center, gap-3
  - Preset name: `<span class="text-[15px] font-medium text-text-primary">{name}</span>`
  - Category: `<span class="text-[13px] text-text-body">{category}</span>`
- **Right side:** flex, items-center, gap-4
  - Genre tag: `<span class="text-xs font-medium text-text-secondary bg-white rounded-full px-3 py-1">{genre}</span>`
  - Play button: `<Play size={18} class="text-text-secondary cursor-pointer" />` (non-functional in this phase)

**"GET ORBITAL NOW" button** below the list:
- `<a href="#" class="mt-4 inline-flex items-center gap-2 rounded-md border border-border-medium px-8 py-3.5 text-[13px] font-medium text-text-secondary tracking-[0.5px] hover:bg-card transition-colors">`
- Text: "GET ORBITAL NOW"
- `<ArrowRight size={16} />`
  </action>
  <verify>Run `npm run build` — no errors. File contains 5 preset names. Grep for "Cosmic Drift" and "Void Lead" confirms all 5 presets.</verify>
  <done>SoundLibrary.astro renders 5 preset rows with name, category, genre tag, and play icon, plus a CTA button. Responsive: rows stack on mobile. Background is #FAFAFA.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Both component files exist in src/components/
- Features has 6 cards with 6 different lucide icons
- SoundLibrary has 5 preset rows with Play icons
- Both sections use the standard section header pattern (label + title + description)
</verification>

<success_criteria>
Features and Sound Library sections are complete, responsive, data-driven, and match the design spec. Components are ready for index.astro composition.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-03-SUMMARY.md`
</output>
