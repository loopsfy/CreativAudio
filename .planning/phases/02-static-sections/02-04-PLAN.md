---
phase: 02-static-sections
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/SocialProof.astro
  - src/components/Pricing.astro
autonomous: true

must_haves:
  truths:
    - "User sees a Social Proof section with 4 stats (10K+, 500+, 4.9/5, 50+) and 3 testimonial cards with star ratings"
    - "User sees a Pricing section with countdown timer placeholder, pricing card with original/sale price, feature list, and CTA"
    - "Both sections are fully responsive"
  artifacts:
    - path: "src/components/SocialProof.astro"
      provides: "Stats row and 3 testimonial cards with star ratings"
      min_lines: 40
    - path: "src/components/Pricing.astro"
      provides: "Timer placeholder, pricing card with prices and feature list"
      min_lines: 50
  key_links:
    - from: "src/components/SocialProof.astro"
      to: "@lucide/astro"
      via: "Star icon for ratings"
      pattern: "Star"
    - from: "src/components/Pricing.astro"
      to: "@lucide/astro"
      via: "Check icon for feature list"
      pattern: "Check"
---

<objective>
Build the Social Proof and Pricing sections matching the Pencil design spec.

Purpose: Social proof builds trust with stats and testimonials. Pricing drives conversion with the launch offer, countdown placeholder, and clear CTA.
Output: SocialProof.astro, Pricing.astro components ready for composition.
</objective>

<execution_context>
@C:/Users/zepid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zepid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-sections/DESIGN-SPEC.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@.planning/phases/02-static-sections/02-01-SUMMARY.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Social Proof section component</name>
  <files>src/components/SocialProof.astro</files>
  <action>
Create `src/components/SocialProof.astro` matching DESIGN-SPEC.md section 5.

**Section container:**
- `<section>` — vertical flex, items-center, gap-12 md:gap-16 (64px)
- Padding: py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]
- Background: white (default)

**Section header:**
- Label: "TRUSTED BY PRODUCERS"
- Title: "What Artists Say" — Orbitron 48px

**Stats row** (id: Ls8Nx):
- `<div class="flex flex-wrap justify-center gap-12 md:gap-20 w-full">` (gap: 80px desktop)
- Data array:
```typescript
const stats = [
  { value: "10K+", label: "Active Users" },
  { value: "500+", label: "Presets" },
  { value: "4.9/5", label: "Rating" },
  { value: "50+", label: "DAW Compatible" },
];
```
- Each stat: vertical flex, items-center, gap-1
  - Value: `<span class="text-3xl md:text-4xl font-heading font-bold text-text-primary">{value}</span>`
  - Label: `<span class="text-sm text-text-body">{label}</span>`

**Testimonials row** (id: chTmB):
- `<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full max-w-[1200px]">`
- Data array:
```typescript
import { Star } from "@lucide/astro";

const testimonials = [
  {
    quote: "Orbital has completely transformed my production workflow. The oscillator engines are unlike anything I've used before.",
    author: "Alex Rivera",
    role: "Electronic Producer"
  },
  {
    quote: "The spectral effects suite alone is worth the price. I've replaced three plugins with just Orbital.",
    author: "Sarah Chen",
    role: "Sound Designer"
  },
  {
    quote: "Best synth plugin I've purchased in years. The preset morphing feature is absolutely game-changing.",
    author: "Marcus Johnson",
    role: "Film Composer"
  },
];
```

**Each testimonial card** (design spec: cornerRadius 12, fill #F0F0F2, stroke #E5E5E7 1px, padding 32, gap 20):
- `<div class="rounded-xl bg-card border border-border p-8 flex flex-col gap-5">`
- **Stars row:** 5 filled stars
  - `<div class="flex gap-1">{[...Array(5)].map(() => <Star size={16} class="text-accent fill-accent" />)}</div>`
  - Note: In Astro, use `{Array.from({length: 5}).map(() => ...)}` syntax
- **Quote:** `<p class="text-[15px] text-text-body leading-relaxed">"{quote}"</p>`
- **Author:** `<div class="flex flex-col gap-0.5"><span class="text-sm font-semibold text-text-primary">{author}</span><span class="text-xs text-text-light">{role}</span></div>`
  </action>
  <verify>Run `npm run build` — no errors. File contains 4 stat items and 3 testimonials. Grep for "10K+" and "Marcus Johnson" confirms all data.</verify>
  <done>SocialProof.astro renders 4 stats in a row and 3 testimonial cards with 5-star ratings, quotes, and author info. Responsive: cards stack on mobile, stats wrap.</done>
</task>

<task type="auto">
  <name>Task 2: Create Pricing section component</name>
  <files>src/components/Pricing.astro</files>
  <action>
Create `src/components/Pricing.astro` matching DESIGN-SPEC.md section 6.

**Section container:**
- `<section>` — vertical flex, items-center, gap-12 (48px)
- Padding: py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]
- Background: radial gradient — `bg-[radial-gradient(ellipse_at_center,_#FFF0E6_0%,_#FFFFFF_70%)]`

**Section header:**
- Label: "PRICING"
- Title: "One Plugin. One Price. No Subscriptions." — Orbitron 48px

**Launch offer countdown** (id: IHIA3):
- Vertical flex, items-center, gap-3
- "LAUNCH OFFER ENDS IN" — text-[11px], font-semibold, text-accent, tracking-[2px]
- **Timer row** (static placeholder — Phase 3 makes it functional):
  - Horizontal flex, gap-3, items-center
  - 3 time boxes (hardcoded: "12" hours, "34" minutes, "56" seconds):
    ```html
    <div class="rounded-lg bg-timer-bg px-4 py-3 flex flex-col items-center gap-0.5 min-w-[60px]">
      <span class="font-mono text-2xl font-bold text-white">12</span>
      <span class="text-[10px] text-text-lighter">HRS</span>
    </div>
    ```
  - Between boxes, separator: `<span class="font-mono text-xl text-border-medium">:</span>`

**Pricing card** (id: cKpb9):
- `<div class="rounded-xl bg-card border border-border p-10 flex flex-col gap-8 w-full max-w-[380px] items-center">`
- Plan name: `<span class="text-sm font-semibold text-accent tracking-[2px]">ORBITAL PRO</span>`
- **Price group:**
  - Original price (crossed out): `<span class="text-lg text-text-light line-through">$299</span>`
  - Sale price: `<span class="text-5xl font-heading font-bold text-text-primary">$149</span>`
  - Period: `<span class="text-sm text-text-body">one-time purchase</span>`
- **Feature list:**
```typescript
import { Check } from "@lucide/astro";

const features = [
  "3 Oscillator Engines",
  "500+ Factory Presets",
  "Spectral Effects Suite",
  "Free Lifetime Updates",
  "Preset Morphing Engine",
  "VST3 / AU / AAX Support",
];
```
  - Each: `<div class="flex items-center gap-3"><Check size={18} class="text-accent shrink-0" /><span class="text-[15px] text-text-body">{feature}</span></div>`
  - Wrap in vertical flex, gap-3, self-stretch

- **CTA button:** Same gradient style as hero CTA
  - `<a href="#" class="w-full inline-flex items-center justify-center gap-2 rounded-md bg-gradient-to-r from-accent to-accent-end px-8 py-4 text-[15px] font-semibold text-white tracking-[1px]">GET ORBITAL<ArrowRight size={18} /></a>`
  - Import ArrowRight from @lucide/astro
  </action>
  <verify>Run `npm run build` — no errors. File contains timer boxes, price values ($299, $149), and 6 feature items. Grep for "LAUNCH OFFER" and "$149" confirms key elements.</verify>
  <done>Pricing.astro renders the launch offer countdown (static), pricing card with original/sale price, 6-item feature list with check icons, and gradient CTA. Responsive: card centers, timer wraps on small screens. Background uses radial gradient.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Both component files exist in src/components/
- SocialProof has 4 stats, 3 testimonial cards, Star icons
- Pricing has timer boxes, $299/$149 prices, 6 feature checks, CTA button
</verification>

<success_criteria>
Social Proof and Pricing sections are complete, responsive, and match the design spec. Timer is static (functional countdown deferred to Phase 3). Components are ready for index.astro composition.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-04-SUMMARY.md`
</output>
