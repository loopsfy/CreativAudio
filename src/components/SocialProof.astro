---
import { Star, BadgeCheck } from "@lucide/astro";

const stats = [
  { value: "2,100+", label: "Producers Trust Us" },
  { value: "175", label: "Presets" },
  { value: "4.9/5", label: "Rating" },
  { value: "5", label: "Built-in Effects" },
];

const testimonials = [
  {
    quote: "the pads on this are beautiful. they have this warm analog feel that just sits in the mix without touching anything. first thing I load every session now",
    author: "Alex Rivera",
    img: "https://images.unsplash.com/photo-1598488035139-bdbb2231ce04?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The keys are so clean and expressive. I use them on literally every beat, they just have that warmth that makes everything sound more professional",
    author: "Sarah Chen",
    img: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The leads cut through the mix so well without any extra EQ. Haven't found another plugin that nails melodies like this one does",
    author: "Marcus Johnson",
    img: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "Those pads are something else \uD83D\uDD25 they fill up a track so nicely without being too much. perfect for setting the mood on any type of beat",
    author: "Lena Krüger",
    img: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "Super intuitive interface, figured everything out in like 2 minutes. And being able to switch skins is a nice touch. Best looking plugin I own honestly",
    author: "Davi Santos",
    img: "https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "the keys have this character that I can't get from any other plugin. every time I open it I end up making a whole new beat from scratch \uD83D\uDE02",
    author: "Jordan Park",
    img: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "every lead sound just works. no fighting with the mix, no clashing. they sit exactly where they need to. saves me so much time",
    author: "Nia Okafor",
    img: "https://images.unsplash.com/photo-1531746020798-e6953c6e8e04?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The pads give my tracks this cinematic feel that I could never get before. my vocalist stopped the session to ask what sound that was \uD83D\uDE4C",
    author: "David Lin",
    img: "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The keys sound warm and natural, not digital at all. Perfect for R&B and soul. Honestly can't believe the quality for this price",
    author: "Mika Tanaka",
    img: "https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "I bought it for the leads but the pads are what I use the most now. they add so much depth to everything. this plugin is a permanent part of my setup",
    author: "Carlos Vega",
    img: "https://images.unsplash.com/photo-1463453091185-61582044d556?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "Other producers always ask what I use for my keys. When I tell them the price they don't believe me. Sound quality is way above what you'd expect \uD83D\uDCAF",
    author: "Emma Whitfield",
    img: "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "honestly didnt expect much but the leads are crazy good. they have this unique character that makes my beats stand out. use it on everything now",
    author: "Tyler Knox",
    img: "https://images.unsplash.com/photo-1560250097-0b93528c311a?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The pads and keys together are unreal. Layer them up and you get these huge textures that sound like a full production. so good",
    author: "Priya Sharma",
    img: "https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "The keys have that lo-fi warmth I've been looking for. Perfect for chill and ambient beats without any extra processing needed \uD83C\uDFB9",
    author: "Jake Morrison",
    img: "https://images.unsplash.com/photo-1492562080023-ab3db95bfbce?w=80&h=80&fit=crop&crop=face",
  },
  {
    quote: "9 out of 10 beats I make have at least one lead or pad from this. the sounds just hit different. best money I've spent on a plugin this year",
    author: "Destiny Hall",
    img: "https://images.unsplash.com/photo-1580489944761-15a19d654956?w=80&h=80&fit=crop&crop=face",
  },
];
---

<section id="reviews" class="flex flex-col items-center gap-12 md:gap-16 py-16 md:py-[100px] px-6 md:px-16 lg:px-[120px]">
  <!-- Section Header -->
  <div class="flex flex-col items-center gap-4" data-animate="fade-up">
    <span class="text-xs font-semibold text-accent tracking-[2px]">TRUSTED BY PRODUCERS</span>
    <h2 class="font-heading text-4xl md:text-[48px] font-bold text-text-primary tracking-[-1px] text-center">What Producers Say</h2>
  </div>

  <!-- Stats Row -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4 w-full max-w-[800px]" data-animate="stagger">
    {stats.map((stat) => (
      <div class="flex flex-col items-center gap-1.5 rounded-xl bg-gradient-to-b from-[#4d2a12] to-[#161618] border border-accent/40 py-6 px-5">
        <span class="text-2xl md:text-3xl font-heading font-bold text-white">{stat.value}</span>
        <span class="text-xs md:text-sm text-text-body tracking-wide">{stat.label}</span>
      </div>
    ))}
  </div>

  <!-- Testimonials Carousel -->
  <div class="w-full max-w-full overflow-hidden relative" id="testimonial-carousel" data-animate="fade-in">
    <!-- Fade edges -->
    <div class="absolute left-0 top-0 bottom-0 w-16 md:w-48 bg-gradient-to-r from-bg to-transparent z-10 pointer-events-none"></div>
    <div class="absolute right-0 top-0 bottom-0 w-16 md:w-48 bg-gradient-to-l from-bg to-transparent z-10 pointer-events-none"></div>

    <div class="flex gap-6 carousel-track">
      {/* Duplicate testimonials for seamless loop */}
      {[...testimonials, ...testimonials].map((testimonial) => (
        <div class="min-w-[340px] max-w-[340px] rounded-xl bg-card border border-border p-8 flex flex-col gap-5 shrink-0">
          <!-- Stars -->
          <div class="flex gap-1">
            {Array.from({ length: 5 }).map(() => (
              <Star size={16} class="text-accent fill-accent" />
            ))}
          </div>
          <!-- Quote -->
          <p class="text-[15px] text-text-body leading-relaxed flex-1">"{testimonial.quote}"</p>
          <!-- Author -->
          <div class="flex items-center gap-3">
            <img
              src={testimonial.img}
              alt={testimonial.author}
              width="40"
              height="40"
              class="w-10 h-10 rounded-full object-cover"
              loading="lazy"
            />
            <div class="flex flex-col gap-0.5">
              <span class="text-sm font-semibold text-text-primary">{testimonial.author}</span>
              <span class="text-xs text-accent flex items-center gap-1">
                <BadgeCheck size={14} class="text-accent" />
                Verified Buyer
              </span>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .carousel-track {
    animation: scroll 10s linear infinite;
  }

  @media (min-width: 768px) {
    .carousel-track {
      animation: scroll 20s linear infinite;
    }
  }

  .carousel-track:hover {
    animation-play-state: paused;
  }

  .carousel-track.dragging {
    animation-play-state: paused;
    cursor: grabbing;
  }

  .carousel-track {
    cursor: grab;
  }

  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
</style>

<script>
  const track = document.querySelector('.carousel-track') as HTMLElement;
  let isDragging = false;
  let startX = 0;
  let offsetAtGrab = 0;

  function getComputedTranslateX() {
    const style = window.getComputedStyle(track);
    const matrix = new DOMMatrix(style.transform);
    return matrix.m41;
  }

  function startDrag(x: number) {
    isDragging = true;
    startX = x;
    // Capture current computed position and freeze animation
    offsetAtGrab = getComputedTranslateX();
    track.style.animation = 'none';
    track.style.transform = `translateX(${offsetAtGrab}px)`;
    track.classList.add('dragging');
  }

  function moveDrag(x: number) {
    if (!isDragging) return;
    const diff = x - startX;
    track.style.transform = `translateX(${offsetAtGrab + diff}px)`;
  }

  function endDrag() {
    if (!isDragging) return;
    isDragging = false;
    track.classList.remove('dragging');
    // Resume CSS animation from current position
    track.style.transform = '';
    track.style.animation = '';
  }

  // Mouse events
  track.addEventListener('mousedown', (e) => {
    e.preventDefault();
    startDrag(e.clientX);
  });
  window.addEventListener('mousemove', (e) => moveDrag(e.clientX));
  window.addEventListener('mouseup', endDrag);

  // Touch events
  track.addEventListener('touchstart', (e) => startDrag(e.touches[0].clientX), { passive: true });
  track.addEventListener('touchmove', (e) => moveDrag(e.touches[0].clientX), { passive: true });
  track.addEventListener('touchend', endDrag);
</script>
