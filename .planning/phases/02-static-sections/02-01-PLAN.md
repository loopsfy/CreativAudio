---
phase: 02-static-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/Layout.astro
  - src/components/SectionDivider.astro
autonomous: true

must_haves:
  truths:
    - "All design spec colors are available as Tailwind utilities (bg-card, text-primary, border-light, etc.)"
    - "Page has SEO meta tags including Open Graph and Twitter cards"
    - "SectionDivider component renders a 1px horizontal line matching design spec"
    - "@lucide/astro is installed and importable"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Updated @theme tokens matching design spec exactly"
      contains: "--color-card: #F0F0F2"
    - path: "src/layouts/Layout.astro"
      provides: "SEO meta tags, font weight imports, ogImage prop"
      contains: "og:title"
    - path: "src/components/SectionDivider.astro"
      provides: "Reusable 1px divider between sections"
      min_lines: 3
  key_links:
    - from: "src/styles/global.css"
      to: "all section components"
      via: "Tailwind utility classes using @theme tokens"
      pattern: "--color-"
    - from: "src/layouts/Layout.astro"
      to: "src/pages/index.astro"
      via: "Layout wrapper with SEO props"
      pattern: "og:title"
---

<objective>
Update design tokens to match the Pencil design spec exactly, extend Layout.astro with full SEO meta tags, install @lucide/astro, and create the SectionDivider component.

Purpose: All section plans (02-05) depend on correct design tokens and the icon library being available. This plan establishes that foundation.
Output: Updated global.css, Layout.astro with SEO, SectionDivider.astro, @lucide/astro installed.
</objective>

<execution_context>
@C:/Users/zepid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zepid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-sections/DESIGN-SPEC.md
@.planning/phases/02-static-sections/02-RESEARCH.md
@src/styles/global.css
@src/layouts/Layout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and install @lucide/astro</name>
  <files>src/styles/global.css, package.json</files>
  <action>
1. Run `npm install @lucide/astro` to add the icon library.

2. Update `src/styles/global.css` — replace the entire `@theme` block to align with the design spec exactly. The current tokens use approximate values (e.g., #F9FAFB, #E5E7EB, #1A1A1A) that differ from the design spec. Replace them with exact spec values and add all missing tokens:

```css
@theme {
  /* Brand */
  --color-accent: #FF5C00;
  --color-accent-light: #FF7A33;
  --color-accent-dark: #CC4A00;
  --color-accent-end: #FF8A4C;

  /* Text */
  --color-text-primary: #111111;
  --color-text-secondary: #555555;
  --color-text-body: #666666;
  --color-text-light: #888888;
  --color-text-lighter: #999999;

  /* Backgrounds */
  --color-bg: #FFFFFF;
  --color-surface: #FAFAFA;
  --color-surface-footer: #F5F5F7;
  --color-card: #F0F0F2;

  /* Borders */
  --color-border: #E5E5E7;
  --color-border-medium: #D4D4D8;

  /* Special */
  --color-timer-bg: #111111;

  /* Typography */
  --font-heading: "Orbitron", sans-serif;
  --font-body: "Inter", sans-serif;
  --font-mono: "DM Mono", monospace;
}
```

Note: This removes `--color-text`, `--color-text-muted`, and `--color-surface` (old approximate values) and replaces with design-spec-accurate names. The body text-color class on `<body>` in Layout.astro needs updating from `text-text` to `text-text-primary`.

3. Add global styles after the @theme block for summary/details marker removal:

```css
summary { list-style: none; }
summary::-webkit-details-marker { display: none; }
```

4. Add DM Mono weight 500 and 700 imports to Layout.astro frontmatter (design spec uses these for compatibility row and timer):

```js
import "@fontsource/dm-mono/500.css";
import "@fontsource/dm-mono/700.css";
```
  </action>
  <verify>Run `npm run build` — should succeed with no errors. Grep global.css for `--color-card: #F0F0F2` and `--color-text-primary: #111111` to confirm tokens are updated.</verify>
  <done>All design spec colors exist as @theme tokens. @lucide/astro is in package.json. DM Mono 500/700 weights are imported. Summary marker styles are set globally.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Layout.astro with SEO meta tags</name>
  <files>src/layouts/Layout.astro</files>
  <action>
Update Layout.astro to add full SEO meta tags. Keep existing font imports. Changes:

1. Update the Props interface to add `ogImage` prop:
```typescript
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}
```

2. Destructure with defaults:
```typescript
const {
  title,
  description = "Orbital - Next-generation audio synthesizer plugin by CreativAudio. 3 oscillator engines, 500+ presets, spectral effects.",
  ogImage = "/og-image.png"
} = Astro.props;
```

3. Add to `<head>` (after existing meta tags, before title):
```html
<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImage} />

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />
```

4. Update `<body>` class from `text-text` to `text-text-primary` (matching new token name).
  </action>
  <verify>Run `npm run build`. Check `dist/index.html` contains `og:title` and `twitter:card` meta tags.</verify>
  <done>Layout.astro has Open Graph and Twitter Card meta tags. Body uses text-text-primary. SEO props are configurable per-page.</done>
</task>

<task type="auto">
  <name>Task 3: Create SectionDivider component</name>
  <files>src/components/SectionDivider.astro</files>
  <action>
Create `src/components/SectionDivider.astro` — a simple horizontal rule matching the design spec:

```astro
---
// SectionDivider: 1px line between major sections
// Design spec: height 1px, fill #E5E5E7, full width
---
<div class="w-full h-px bg-border" aria-hidden="true"></div>
```

This renders a full-width 1px divider using the `--color-border` token (#E5E5E7). `aria-hidden` since it's decorative.
  </action>
  <verify>File exists at src/components/SectionDivider.astro. Run `npm run build` to confirm no import errors.</verify>
  <done>SectionDivider.astro exists and renders a 1px #E5E5E7 horizontal divider.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `grep -c "og:title" dist/index.html` returns 1
- `grep -c "--color-card" src/styles/global.css` returns 1
- `@lucide/astro` appears in package.json dependencies
- `src/components/SectionDivider.astro` exists
</verification>

<success_criteria>
Design tokens match the Pencil spec exactly. Layout has SEO meta. @lucide/astro is installed. SectionDivider is ready. All subsequent plans can use the correct token names and icons.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-01-SUMMARY.md`
</output>
